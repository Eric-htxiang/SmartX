% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{drawUMAP}
\alias{drawUMAP}
\title{Draw UMAP plots for Seurat object}
\usage{
drawUMAP(
  seuObj,
  groupBy,
  reducName,
  sampleIDs = NULL,
  colorCodes,
  drawSingleGroup = FALSE,
  prefix,
  pltHeight = 8,
  pltWidth = 8,
  pointSize = 0.1,
  legCol = NULL,
  labBox = TRUE,
  labColor = "white",
  labSize = 3
)
}
\arguments{
\item{seuObj}{A Seurat object with dimensional reduction computed}

\item{groupBy}{Character string specifying metadata column for grouping}

\item{reducName}{Character string specifying reduction name (e.g., "umap", "tsne")}

\item{sampleIDs}{Optional character vector of sample IDs to plot separately}

\item{colorCodes}{Named vector of colors for each group}

\item{drawSingleGroup}{Logical, whether to create individual plots for each group (default: FALSE)}

\item{prefix}{Character string for output filename prefix}

\item{pltHeight}{Numeric, plot height in inches (default: 8)}

\item{pltWidth}{Numeric, plot width in inches (default: 8)}

\item{pointSize}{Numeric, point size for cells (default: 0.1)}

\item{legCol}{Integer, number of legend columns (default: auto-calculated)}

\item{labBox}{Logical, whether to draw boxes around labels (default: TRUE)}

\item{labColor}{Character string, label color (default: "white")}

\item{labSize}{Numeric, label size (default: 3)}
}
\value{
NULL. Saves PDF file with UMAP plots
}
\description{
Creates comprehensive UMAP visualizations including overall clustering,
individual cluster highlighting, and sample-specific plots.
}
\details{
The function creates three types of plots:
\enumerate{
\item Overall UMAP with all groups colored
\item Individual plots highlighting each group (if drawSingleGroup = TRUE)
\item Sample-specific plots (if sampleIDs provided)
}

Output filename: \code{<prefix>_<groupBy>_<reducName>_UMAP.pdf}
}
\examples{
\dontrun{
# Basic usage
colors <- c("0" = "red", "1" = "blue", "2" = "green")
drawUMAP(seurat_obj, 
         groupBy = "seurat_clusters",
         reducName = "umap",
         colorCodes = colors,
         prefix = "MyProject")

# With individual group plots
drawUMAP(seurat_obj,
         groupBy = "cell_type",
         reducName = "umap",
         colorCodes = colors,
         drawSingleGroup = TRUE,
         prefix = "CellTypes")
}
}
