% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_analysis.R
\name{degOverlapJaccard}
\alias{degOverlapJaccard}
\title{Calculate Jaccard similarity based on DEG overlap}
\usage{
degOverlapJaccard(
  markers1,
  markers2,
  groupBy1,
  groupBy2,
  groupIDs1_order = NULL,
  groupIDs2_order = NULL,
  pCutoff = 0.05,
  FCcutoff = 0.5,
  pct1Cutoff = 0.1,
  pct2Cutoff = 0.01,
  topGeneNum = 100,
  prefix
)
}
\arguments{
\item{markers1}{DEG dataframe from first dataset (from run_DEG)}

\item{markers2}{DEG dataframe from second dataset (from run_DEG)}

\item{groupBy1}{Character string specifying grouping column in markers1}

\item{groupBy2}{Character string specifying grouping column in markers2}

\item{pCutoff}{Numeric, adjusted p-value cutoff (default: 0.05)}

\item{FCcutoff}{Numeric, log2 fold change cutoff (default: 0.5)}

\item{pct1Cutoff}{Numeric, minimum percentage in group 1 (default: 0.1)}

\item{pct2Cutoff}{Numeric, minimum percentage in group 2 (default: 0.01)}

\item{topGeneNum}{Integer, number of top genes to use per group (default: 100)}

\item{prefix}{Character string for output filename prefix}
}
\value{
A similarity matrix with Jaccard indices
}
\description{
Computes Jaccard similarity coefficients between groups based on overlap
of their top differentially expressed genes.
}
\details{
Jaccard index = |intersection| / |union|

Also saves a TSV file with overlapping genes for each pair of groups.
}
\examples{
\dontrun{
markers1 <- run_DEG(dataset1, groupBy = "seurat_clusters")
markers2 <- run_DEG(dataset2, groupBy = "cell_type")

jaccard_matrix <- degOverlapJaccard(
  markers1, markers2,
  groupBy1 = "seurat_clusters",
  groupBy2 = "cell_type",
  topGeneNum = 100,
  prefix = "Dataset_Comparison"
)
}
}
